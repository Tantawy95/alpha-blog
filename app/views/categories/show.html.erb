<h1 class="ml-4 mt-4">Category: <%= @category.name%></h1>

<div class="ml-4 mt-4">
  <% if logged_in?%>
    <%= link_to "Edit category name", edit_category_path(@category), class: "btn btn-outline-info" if current_user.admin  %>
  <%end%> 
</div>

<h2 class="text-center mt-4">Articles</h2>

<div class="flickr_pagination row justify-content-center mt-4">
  <%= will_paginate @articles, :container => false %>
</div>

<%= render 'articles/article' %>

<div class="flickr_pagination row justify-content-center">
  <%= will_paginate @articles, :container => false %>
</div>